import {
    StyleSheet,
} from 'react-native';

export const GLOBAL_STYLES = StyleSheet.create({
    _mainContainer:{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    }
})